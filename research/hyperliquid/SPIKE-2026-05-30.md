# Hyperliquid Data Feasibility Spike — 2026-05-30

> Archived evidence for the copy-trading blow-up paper (spec: `docs/superpowers/specs/2026-05-30-copytrading-drift-paper-design.md`).
> Purpose: go/no-go on whether the official Hyperliquid public API yields the ground truth + behavioral signals the paper needs. **Verdict: GO.**

## Method
Live POST to `https://api.hyperliquid.xyz/info` (no auth, Content-Type: application/json), probe address = **James Wynn `0xBC476160765ff70CBD78D5A63AaA885555d575F1`** (publicly self-doxxed influencer; used here only as a feasibility probe, NOT in the statistical cohort per spec §6).

## Verified endpoints & fields

| Endpoint (request `type`) | Need | Verified fields |
|---|---|---|
| `userFillsByTime` / `userFills` | tick-by-tick trades | `closedPnl`, `coin`, `dir`, `px`, `side`, `sz`, `startPosition`, `time`, `crossed`, `fee`, `tid` + **`liquidation` key present on liquidation fills** |
| `portfolio` | equity curve | `accountValueHistory`, `pnlHistory`, `vlm` across day/week/month/allTime + perp* periods |
| `historicalOrders` | stop/trigger orders | `isTrigger`, `triggerPx`, `triggerCondition`, `isPositionTpsl`, `reduceOnly`, `orderType`, `origSz`, `status`, `statusTimestamp` |
| `userNonFundingLedgerUpdates` | margin top-up behavior | `delta.type` ∈ {deposit, withdraw, …} with `usdc`/amount + `time` |
| `clearinghouseState` | current account state | `marginSummary.accountValue`, `assetPositions` (sanity) |

## Key evidence (James Wynn probe)
- **Ground truth retrievable**: full-history fill scan returned `any fill with 'liquidation' key? True`. Blow-up event time is recoverable at per-fill precision. (Note: liquidation is a per-fill `liquidation` key, NOT a `dir` string — `dir` stays "Close Long" etc.)
- **Equity crater**: `portfolio` perpAllTime peak **$1,895,650 @ 2025-06-11 23:22 → $0** terminal.
- **Tilt / desperation signal**: `userNonFundingLedgerUpdates` = **41 deposit/withdraw events**; e.g. 19 margin top-ups in ~6 hours on 2025-06-05 ($19k → $33k → $18k → $37k …). Serial margin-topping into a losing position = textbook behavioral drift.
- **Volume**: ~12,000 fills in one week (2025-06-05 → 06-12), heavy Open Long / Close Long churn.

## Caveats (carried into spec as design constraints)
- **10k-most-recent-fills cap** per address. HFT-class masters (Wynn ≈ 12k fills/week) → only ~1 week of pre-event baseline. Handled by spec §6 minimum-baseline inclusion rule; exclusion count to be reported as a named selection-bias line (spec §14 phase-1 exit gate).
- Reproducibility: the 10k window rolls forward, so live re-query is non-reproducible → spec §11 requires archiving raw responses with query timestamps in build phase 1.
- On-chain crypto perps only; no generalization to forex/equity copy desks claimed (spec §10).

## Conclusion
All five data requirements (per-trade fills, liquidation ground truth, equity curve, stop/trigger orders, margin-topup behavior) are retrievable from the free public API. **GO** for the real-data flagship.
